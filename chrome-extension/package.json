{
  "name": "arxiv-tab-tracker-extension",
  "version": "1.0.0",
  "description": "Chrome extension to track arXiv tabs and connect to OneNote.",
  "main": "popup.js",
  "scripts": {
    "build": "npx esbuild popup.js --bundle --outfile=popup.bundle.js --minify --loader:.js=jsx"
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.9"
  }
}
